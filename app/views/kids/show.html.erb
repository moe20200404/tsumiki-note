<%= render partial: "shared/nursery_menu.erb" %>

<div class="wrapper">
  <ul class="kid_lists">
  <%= render partial: "shared/kid_show.erb",locals: {kid: @kid}  %>
  </ul>

<%= render partial: "shared/growth_show.erb",locals: {growths: @growths} %>

    <br/>

  <% if current_user.authority_id == 3%>
    <%= link_to '身長測定データを新しく登録する', new_kid_growth_path(kid_id: @kid.id) %>
  <% end %>

</div>