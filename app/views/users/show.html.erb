<%= render partial: "shared/nursery_menu.erb" %>

<div class="wrapper">

  <ul class="user_lists">

  <%= render partial: "shared/user_show.erb",locals: {user: @user}  %>

  </ul>

  <% if @kids.present? %>
  <p>園児</p>

  <% else %>
  <p>あなたの子供は、まだ登録されていません。</p>
  
  <% end %>

  <% @kids.each do |kid| %>

  <%= render partial: "shared/kid_show.erb",locals: {kid: kid}  %>

  <% end%>

</div>